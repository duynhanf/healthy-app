export interface Exercise {
  id: string;
  title: string;
  recordedAt: string;
  burnedCalo: number;
}

export interface Diary {
  id: string;
  title: string;
  recordedAt: string;
  description: string;
}
